services:

  notes_api:
    build:
      context: .
      dockerfile: Dockerfile

    image: msjapp-notes_api:latest

    container_name: fastapi-notes

    ports:
      - "8000:8000"

    volumes:
      - notes-data:/data

    restart: unless-stopped

volumes:
  notes-data: